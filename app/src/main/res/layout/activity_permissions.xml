<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/surface_variant"
        app:title="@string/permissions"
        app:navigationIcon="@drawable/ic_close" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="20dp">

        <include layout="@layout/row_permission" android:id="@+id/rowAccessibility" />

        <include layout="@layout/row_permission" android:id="@+id/rowOverlay" />

        <include layout="@layout/row_permission" android:id="@+id/rowNotifications" />
    </LinearLayout>
</LinearLayout>
